id: 14348
name: emojicdn
environments:
    production:
        domain: emojicdn.elk.sh
        gateway-version: 2
        memory: 1024
        cli-memory: 512
        runtime: php-7.4
        build:
            - 'COMPOSER_MIRROR_PATH_REPOS=1 composer install --no-dev'
            - 'php artisan event:cache'
